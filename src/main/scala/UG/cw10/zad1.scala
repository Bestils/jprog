package UG.cw10

object zad1  extends  App{
//	Korzystając z metod oferowanych przez kolekcje trawersowalne (Traversable[A])
  //	i/lub iterowalne (Iterable[A]) oraz operacji na ciągach zdefiniuj metodę oceniania ruchów dla gry MaterMind (zasady).
  //Przykład:
  //val secret = Seq(1, 3, 2, 2, 4, 5)
  //val guess = Seq(2, 1, 2, 4, 7, 2)
  //
  //val black = /* ... */
  //val white = = /* ... */ - black
  //black, powinien otrzymać 1, white, powinien otrzymać 2.

  val secret = Seq(1, 3, 2, 2, 4, 5)
  val guess = Seq(2, 1, 2, 4, 7, 2)

}
